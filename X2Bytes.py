#!/usr/bin/python
# -*- coding: utf-8 -*-
import os
import system

tobytes = \
    raw_input('Enter the value you need converted from char to bytes: ')
len(tobytes.encode('utf8'))


def pause():
    programPause = raw_input('Press the <ENTER> key to continue...')


pause()

			